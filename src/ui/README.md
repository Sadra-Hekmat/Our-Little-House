# UI boundary

Accessible dialogue, pause, touch-control, orientation, and recovery overlays belong here. UI
modules consume typed view models and publish user intents; they do not import Phaser scenes,
systems, maps, or application routing.
